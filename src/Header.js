import React from 'react';

const Header = () => {
    return (
        <div>
            <h1>Taask Tracker</h1>
            <title>Task tracker</title>
        </div>
    );
};

export default Header;